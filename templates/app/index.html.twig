{% extends 'base.html.twig' %}

{% block body %}

    {% for mensaje in app.session.flashbag().get('info') %}
        <p>{{ mensaje }}</p>
    {% endfor %}
    {{ form_start(form) }}
    {{ form_end(form) }}
{% endblock %}
